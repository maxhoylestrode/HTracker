// Preset theme names a user can pick on the Settings page. "dark" is the
// original app look and stays the default for existing accounts.
const ALLOWED_THEMES = ['dark', 'light', 'pink', 'purple', 'silver'];

module.exports = { ALLOWED_THEMES };
